import React, { Component } from "react";
import { auth } from "../actions";
import { connect } from "react-redux";

export default function(ComposedClass, reload) {
  class AutheticationCheck extends Component {
    state = {
      loading: true
    };
  }
}
